//bài tập về nhà
for (let i = 0; i < 5; i++) {
    if (i % 2 === 0) {
        console.log(`Số ${i} là số chẵn`);
    } else {
        console.log(`Số ${i} là số lẻ`);
    }
}

// bài tập trên lớp. Thử với in ra 3 dòng
for (let i = 0; i < 5; i++) {
    console.log(`${i * 3} Xin chao`)
    console.log(`${i * 3 + 1} Xin chao`)
    console.log(`${i * 3 + 2} Xin chao`)

}